"""
Configuration file for Fantastical Calendar Events Alfred Workflow

SETUP INSTRUCTIONS:
1. Copy this file to 'config.py' (without .template extension)
2. Add your OpenAI API key below
3. The config.py file will be automatically ignored by git for security

Alternative: Set environment variable OPENAI_API_KEY instead of using this file
"""

# Your OpenAI API Key
# Get one from: https://platform.openai.com/api-keys
OPENAI_API_KEY = "sk-your-openai-api-key-here"

# Optional: Customize OpenAI settings
OPENAI_MODEL = "gpt-4"  # or "gpt-3.5-turbo" for faster/cheaper responses
OPENAI_MAX_TOKENS = 200
OPENAI_TEMPERATURE = 0.1  # Low temperature for consistent formatting

# Optional: Fantastical settings
DEFAULT_CALENDAR = None  # None = use default calendar, or specify calendar name
ADD_IMMEDIATELY = True  # Set to False to review events before adding